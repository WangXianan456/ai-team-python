# Python Team Profile

## Scope

- Backend and automation projects in Python.

## Default Toolchain

- `python`, `pip`, `pytest`
- `ruff` / `flake8` (lint)
- `pip-audit` (dependency security)

## Quality Gate Additions

1. Unit tests for changed modules
2. Type/contract checks where applicable
3. Dependency audit before release

